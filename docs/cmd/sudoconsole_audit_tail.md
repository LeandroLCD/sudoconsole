## sudoconsole audit tail

Print the last N entries from the audit log

### Synopsis

tail prints the last N records of the JSONL audit log written
by sudoconsole. The path is taken from the loaded configuration
(policy.audit.log_file). Defaults to 20 records.

```
sudoconsole audit tail [flags]
```

### Examples

```
  sudoconsole audit tail
  sudoconsole audit tail --count 5
  sudoconsole --format json audit tail --count 50
```

### Options

```
      --count int   number of recent records to display (default 20)
  -h, --help        help for tail
```

### Options inherited from parent commands

```
      --cache-timeout int   sudo cache timeout in seconds (60-3600); overrides config file
      --config string       path to the TOML config file (default: platform XDG/Application Support location)
      --format string       output format: human or json (overrides config file)
      --log-level string    log level: silent, error, warn, info, debug (overrides config file)
```

### SEE ALSO

* [sudoconsole audit]()	 - Inspect the audit log

###### Auto generated by spf13/cobra on 1-Aug-2026
