## sudoconsole exec

Run a command under sudo, applying the policy

### Synopsis

exec runs the supplied command under sudo. The command line is
evaluated against the active policy; on Block the command does not run
unless --policy-override <reason> is supplied. When --policy-override is
used, sudoconsole prompts for confirmation (skippable with --yes) and
records the reason in the audit log. The cache is consulted first; if
expired, exec either fails fast (no secret supplied) or silently
refreshes via the password supplied via $SUDOCONSOLE_PASSWORD.

```
sudoconsole exec [--] <cmd...> [flags]
```

### Examples

```
  sudoconsole exec apt update
  sudoconsole exec --policy-override "debugging ssh tunnel" systemctl restart nginx
  sudoconsole --format json exec apt update
```

### Options

```
  -h, --help                     help for exec
      --policy-override string   bypass a Block decision and proceed anyway; the value is recorded in the audit log as the override reason (interactive confirm required unless --yes)
      --user string              sudo --user override (advanced)
      --yes                      skip the interactive confirmation prompt
```

### Options inherited from parent commands

```
      --cache-timeout int   sudo cache timeout in seconds (60-3600); overrides config file
      --config string       path to the TOML config file (default: platform XDG/Application Support location)
      --format string       output format: human or json (overrides config file)
      --log-level string    log level: silent, error, warn, info, debug (overrides config file)
```

### SEE ALSO

* [sudoconsole]()	 - Secure sudo wrapper for CLI agents

###### Auto generated by spf13/cobra on 1-Aug-2026
