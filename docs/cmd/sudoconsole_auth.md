## sudoconsole auth

Authenticate with sudo and cache the credentials

### Synopsis

auth prompts for the sudo password on a secure PTY and
caches the credentials for the configured timeout. Safe to run from any
script; the password is never echoed, logged or written to disk.

```
sudoconsole auth [flags]
```

### Examples

```
  # interactive prompt
  sudoconsole auth

  # non-interactive (testing only) via env var
  SUDOCONSOLE_PASSWORD=secret sudoconsole auth --no-tty
```

### Options

```
  -h, --help     help for auth
      --no-tty   read the password from $SUDOCONSOLE_PASSWORD instead of the TTY (testing only)
```

### Options inherited from parent commands

```
      --cache-timeout int   sudo cache timeout in seconds (60-3600); overrides config file
      --config string       path to the TOML config file (default: platform XDG/Application Support location)
      --format string       output format: human or json (overrides config file)
      --log-level string    log level: silent, error, warn, info, debug (overrides config file)
```

### SEE ALSO

* [sudoconsole]()	 - Secure sudo wrapper for CLI agents

###### Auto generated by spf13/cobra on 1-Aug-2026
