## sudoconsole install

Install sudoconsole adapters for detected CLI agents

### Synopsis

install registers sudoconsole with every detected CLI agent
(Kilo, Claude Code, Gemini, Aider, Codex, Copilot). The operation is
idempotent: running it twice yields the same state. Use --force to
overwrite an existing integration, --dry-run to preview the changes,
--kind to restrict the operation to a specific agent, and --yes to skip
the interactive confirmation.

```
sudoconsole install [flags]
```

### Examples

```
  sudoconsole install
  sudoconsole install --kind kilo --kind claude
  sudoconsole install --dry-run --force
```

### Options

```
      --bin-dir string       override the bin directory for wrappers (default: config agent.bin_dir)
      --dry-run              report what would change without touching the filesystem
      --force                overwrite existing integration files
  -h, --help                 help for install
      --kind strings         agent kinds to install (kilo, claude, gemini, aider, codex, copilot, generic)
      --policy-mode string   override the policy mode for this invocation (blocklist|allowlist|audit)
      --yes                  skip the interactive confirmation prompt
```

### Options inherited from parent commands

```
      --cache-timeout int   sudo cache timeout in seconds (60-3600); overrides config file
      --config string       path to the TOML config file (default: platform XDG/Application Support location)
      --format string       output format: human or json (overrides config file)
      --log-level string    log level: silent, error, warn, info, debug (overrides config file)
```

### SEE ALSO

* [sudoconsole]()	 - Secure sudo wrapper for CLI agents
* [sudoconsole install uninstall]()	 - Remove sudoconsole adapters from every detected CLI agent

###### Auto generated by spf13/cobra on 1-Aug-2026
