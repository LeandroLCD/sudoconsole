## sudoconsole policy list

List every binary classified by the policy engine

### Synopsis

list dumps the (binary → category → risk) table used by the
evaluator. Built-in entries ship with sudoconsole; entries added via
[policy.custom] are flagged with builtin=false.

```
sudoconsole policy list [flags]
```

### Examples

```
  sudoconsole policy list
  sudoconsole --format json policy list
```

### Options

```
  -h, --help   help for list
```

### Options inherited from parent commands

```
      --cache-timeout int   sudo cache timeout in seconds (60-3600); overrides config file
      --config string       path to the TOML config file (default: platform XDG/Application Support location)
      --format string       output format: human or json (overrides config file)
      --log-level string    log level: silent, error, warn, info, debug (overrides config file)
```

### SEE ALSO

* [sudoconsole policy]()	 - Inspect and validate the active policy

###### Auto generated by spf13/cobra on 1-Aug-2026
